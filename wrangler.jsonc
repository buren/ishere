/**
 * For more details on how to configure Wrangler, refer to:
 * https://developers.cloudflare.com/workers/wrangler/configuration/
 */
{
	"$schema": "node_modules/wrangler/config-schema.json",
	"name": "ishere-v2",
	"main": "src/index.ts",
	"compatibility_date": "2025-04-08",

	// Logs
	"observability": {
		"enabled": true
	},

	// Dev configuration
  "vars": {
    "ENVIRONMENT": "development",
    "ACCOUNT_ID": "TODO" // Clouudflare Account ID
  },

  // "routes": [
  //   { "pattern": "dev.wshr.io", "custom_domain": true }
  // ],

  "kv_namespaces": [
    { "binding": "KV", "id": "TODO", "preview_id": "TODO" }
  ],

  "d1_databases": [
    {
			"binding": "D1",
			"database_name": "ishere",
			"database_id": "TODO",
			"preview_database_id": "TODO"
		}
  ],

  "analytics_engine_datasets": [
    { "binding": "REDIRECTS", "dataset": "ishere-redirects" }
  ],

  "triggers": {
    "crons": [ "0 * * * *" ]
  },

  // Production configuration
  "env": {
    "production": {
      "vars": {
        "ENVIRONMENT": "production",
        "ACCOUNT_ID": "TODO"
      },

      "routes": [
        { "pattern": "wshr.io", "custom_domain": true }
      ],

      "analytics_engine_datasets": [
        { "binding": "REDIRECTS", "dataset": "ishere-redirects-production" }
      ],

      "triggers": {
        "crons": [ "0 * * * *" ]
      }
    }
  }
}
